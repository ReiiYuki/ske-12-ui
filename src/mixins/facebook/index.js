import colors from 'mixins/colors'

export const facebookButtonColorSet = {
	textColor: colors.white,
	buttonColors: {
		disable: colors.fb50,
		normal: colors.fb,
		hover: colors.fb100,
		press: colors.fb200,
	},
}
